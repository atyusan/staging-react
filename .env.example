VITE_DEV_SERVER_PORT=5173
VITE_PREVIEW_PORT=4173
# Dev/preview only: backend URL for the Vite proxy
VITE_API_PROXY_TARGET=http://localhost:4001
# Production build: full backend API base URL (required when frontend and backend are on separate hosts)
# VITE_API_URL=https://your-backend.example.com/api
